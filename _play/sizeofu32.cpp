#include <iostream>
#include <stdint.h>

int main()
{
  std::cout << "sizeof(long): " << sizeof(long) << std::endl;
  std::cout << "sizeof(uint32_t): " << sizeof(uint32_t) << std::endl;

  return 0;
}
